{
  "rules": {
    "codigosActivacion": {
      ".read": "auth != null",
      ".write": false
    },
    "userAlarma": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && !data.exists()"
      }
    },
    "alarmas": {
      "$alarmaId": {
        "usuarios": {
          ".read": "auth != null && root.child('userAlarma').child(auth.uid).val() === $alarmaId",
          ".write": false,
          "$uid": {
            ".write": "auth != null && (auth.uid === $uid || root.child('userAlarma').child(auth.uid).val() === $alarmaId)"
          }
        },
        "dispositivoUid": {
          ".read": "auth != null && root.child('userAlarma').child(auth.uid).val() === $alarmaId",
          ".write": false
        },
        "historial": {
          ".read": "auth != null && root.child('userAlarma').child(auth.uid).val() === $alarmaId",
          ".write": "auth != null && (root.child('userAlarma').child(auth.uid).val() === $alarmaId || root.child('alarmas').child($alarmaId).child('dispositivoUid').val() === auth.uid)"
        }
      }
    }
  }
}
