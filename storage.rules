rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite subir/leer archivos solo a usuarios autenticados (incluye el ESP con email/password).
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}